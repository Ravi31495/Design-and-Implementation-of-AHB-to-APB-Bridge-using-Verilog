# ModelSim

The original project archive contains the ModelSim project configuration (`ahb2apb.mpf`) along with generated simulator state. Generated waveform databases are intentionally not committed.

Compile/run the Verilog sources under `src/bridge/` and `sim/`, then start `top_tb` and inspect the AHB/APB transaction signals.
